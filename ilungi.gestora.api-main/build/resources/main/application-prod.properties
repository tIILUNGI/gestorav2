# ========================
# SERVER CONFIGURATION
# ========================
server.port=${PORT:8080}
server.address=0.0.0.0

# ========================
# SPRING APPLICATION
# ========================
spring.application.name=ilungi-gestora-api

# ========================
# DATABASE CONFIGURATION
# ========================
spring.datasource.url=jdbc:postgresql://dpg-d5seeg95pdvs739l3vi0-a.oregon-postgres.render.com/gestora_db
spring.datasource.username=gestora_db_user
spring.datasource.password=k02iSs2209S19mIYROyxUzqVPm3w2mNY
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000


#========= Email Service=======#
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=wannabedevolop60@gmail.com
spring.mail.password=zafo hwwr rvqn xixo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.email.sender=noreply@gestora.com
app.email.admin=admin@gestora.com



# ========================
# JPA / HIBERNATE
# ========================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=10
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.generate_statistics=false
spring.jpa.open-in-view=false

# ========================
# LOGGING
# ========================
logging.level.root=WARN
logging.level.com.ilungi=INFO
logging.level.org.springframework=WARN
logging.level.org.hibernate=ERROR
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# ========================
# SECURITY (JWT)
# ========================
# Configure via environment variables
#jwt.secret=${JWT_SECRET}
#jwt.expiration=${JWT_EXPIRATION:86400000}

# ========================
# ACTUATOR (HEALTH CHECKS)
# ========================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
management.endpoint.health.probes.enabled=true

# ========================
# CORS (se não configurado no código)
# ========================
#spring.web.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
#spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
#spring.web.cors.allowed-headers=*
#spring.web.cors.allow-credentials=true

# ========================
# TOMCAT (otimizações)
# ========================
server.tomcat.max-threads=20
server.tomcat.min-spare-threads=5
server.tomcat.max-connections=100
server.tomcat.accept-count=50
server.tomcat.connection-timeout=5000

# ========================
# COMPRESSION
# ========================
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
server.compression.min-response-size=2048

# ========================
# FILE UPLOAD
# ========================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ========================
# EMAIL (se configurado)
# ========================
#spring.mail.host=${MAIL_HOST}
#spring.mail.port=${MAIL_PORT:587}
#spring.mail.username=${MAIL_USERNAME}
#spring.mail.password=${MAIL_PASSWORD}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true